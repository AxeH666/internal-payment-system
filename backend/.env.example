# Phase 1.8 — Docker & PostgreSQL env template
# Copy this file to .env at PROJECT ROOT (same directory as docker-compose.yml)
# so Docker Compose can read it. Do not commit .env.
#
# Optional: copy to backend/.env for local backend runs.

# PostgreSQL (required for docker-compose)
POSTGRES_DB=internal_payment
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# Host/port for app when connecting to Postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Optional: pgAdmin (only if using docker-compose --profile tools)
# PGADMIN_EMAIL=admin@local.local
# PGADMIN_PASSWORD=
